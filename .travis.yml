language: ruby
rvm:
  - '2.2.4'

addons:
  postgresql: "9.3"

services:
  - redis-server

gemfile:
  - Gemfile

matrix:

before_script:
  - psql -c 'create database lintci_test;' -U postgres

notifications:
  email: false
